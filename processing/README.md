# Processing

Images currently being processed are moved here from `input/`.

During processing, the pipeline generates intermediate outputs alongside the image. Once processing completes, the image and its outputs move to `review/`.
