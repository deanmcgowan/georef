# Review

Images that have been processed and are awaiting manual validation.

Each image will have an accompanying HTML report and VRT file in `reports/`. Open the report to assess quality, then load the VRT in QGIS to verify alignment visually. Once satisfied, move the image to `done/`.
