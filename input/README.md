# Input

Place scanned map images here for georeferencing. Supported formats: JPG, PNG, TIFF.

Each image should be a scanned map containing printed coordinate crosses that can be detected by template matching. The user must provide the source CRS, grid origin, and grid spacing when invoking the processing script.
